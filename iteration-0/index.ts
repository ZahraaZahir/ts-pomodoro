import { TimerSettings } from "./TimerSettings"

const enumTest = console.log(`Here is the enumeration for breaks: ${TimerSettings.shortBreak}, ${TimerSettings.midBreak}, ${TimerSettings.longBreak}, and ${TimerSettings.hourBreak}`)
